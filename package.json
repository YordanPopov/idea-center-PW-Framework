{
  "name": "idea-center-pw-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:testing": "cross-env ENVIRONMENT=testing npx playwright test",
    "smoke": "npx playwright test --grep @Smoke --reporter=blob --output=smoke-blob-report",
    "api": "npx playwright test --grep @Api --reporter=blob --output=api-blob-repor"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/dotenv": "^8.2.3",
    "@types/node": "^24.3.0",
    "cross-env": "^10.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "zod": "^4.1.8"
  }
}
